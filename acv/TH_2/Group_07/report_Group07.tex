
% !TEX program = xelatex
% Compile:
%   xelatex -interaction=nonstopmode report_Group07.tex
%   xelatex -interaction=nonstopmode report_Group07.tex
\documentclass[12pt,a4paper]{article}

% ===== Page setup =====
\usepackage[a4paper,top=2.2cm,bottom=2.2cm,left=2.5cm,right=2.0cm]{geometry}
\usepackage{fontspec}
\IfFontExistsTF{Times New Roman}{
  \setmainfont{Times New Roman}
  \setsansfont{Times New Roman}
}{
  \IfFontExistsTF{TeX Gyre Termes}{
    \setmainfont{TeX Gyre Termes}
    \setsansfont{TeX Gyre Termes}
  }{
    \setmainfont{Tinos}
    \setsansfont{Tinos}
  }
}
\setmonofont{DejaVu Sans Mono}
\XeTeXlinebreaklocale "vi"
\XeTeXlinebreakskip = 0pt plus 1pt

% ===== Packages =====
\usepackage{graphicx}
\usepackage{tabularx}
\usepackage{array}
\usepackage{booktabs}
\usepackage{float}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{amsmath}
\usepackage[table]{xcolor}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{lastpage}
\usepackage{listings}
\usepackage{ragged2e}

% ===== Style =====
\definecolor{headingblue}{RGB}{31,78,121}
\definecolor{subheading}{RGB}{64,96,120}
\definecolor{tablehead}{RGB}{221,235,247}
\definecolor{softgray}{RGB}{245,247,250}
\definecolor{rulegray}{RGB}{211,219,228}
\hypersetup{colorlinks=true, linkcolor=black, urlcolor=blue, citecolor=black}
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}
\setlength{\tabcolsep}{5pt}
\linespread{1.04}
\setlist[itemize]{leftmargin=1.5em,itemsep=2pt,topsep=2pt}
\renewcommand{\arraystretch}{1.22}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\sloppy
\emergencystretch=2em

\titleformat{\section}
  {\color{headingblue}\bfseries\fontsize{15pt}{18pt}\selectfont}
  {\thesection.}{0.5em}{}
\titleformat{\subsection}
  {\color{subheading}\bfseries\fontsize{13pt}{16pt}\selectfont}
  {\thesubsection.}{0.5em}{}
\titleformat{name=\subsection,numberless}
  {\color{subheading}\bfseries\fontsize{13pt}{16pt}\selectfont}
  {}{0pt}{}
\titlespacing*{\section}{0pt}{14pt}{7pt}
\titlespacing*{\subsection}{0pt}{8pt}{3pt}
\captionsetup{font=small,labelfont=bf}
\renewcommand{\figurename}{Hình}
\renewcommand{\tablename}{Bảng}

\pagestyle{fancy}
\fancyhf{}
\lhead{\small Group\_07 - Báo cáo thực hành}
\rhead{\small Thị giác máy tính nâng cao}
\cfoot{\thepage/\pageref{LastPage}}
\setlength{\headheight}{16pt}
\renewcommand{\headrulewidth}{0.4pt}

\newcommand{\thead}[1]{\cellcolor{tablehead}\textbf{#1}}
\newcommand{\code}[1]{{\ttfamily\scriptsize\color{headingblue}#1}}
\newcommand{\important}[1]{\textbf{#1}}
\newcommand{\imgmaybe}[2]{%
  \IfFileExists{#1}{\includegraphics[width=\linewidth]{#1}}{%
    \setlength{\fboxsep}{10pt}\colorbox{softgray}{\begin{minipage}[c][4.2cm][c]{0.90\linewidth}\centering\small Chưa tìm thấy hình: \code{#2}\end{minipage}}%
  }%
}
\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  columns=fullflexible,
  frame=single,
  backgroundcolor=\color{softgray},
  xleftmargin=2pt,
  xrightmargin=2pt
}

\begin{document}

\begin{center}
{\color{headingblue}\bfseries\fontsize{18pt}{24pt}\selectfont BÁO CÁO BÀI THỰC HÀNH}\par
\vspace{0.35em}
{\bfseries\fontsize{16pt}{22pt}\selectfont PHÂN ĐOẠN VÀ PHÂN LỚP ẢNH Y KHOA}\par
\vspace{0.85em}
{\color{rulegray}\rule{0.72\textwidth}{0.8pt}}\par
\vspace{0.7em}
{\fontsize{13pt}{18pt}\selectfont Môn: Thị giác máy tính nâng cao}\par
{\fontsize{13pt}{18pt}\selectfont Nhóm: \textbf{Group\_07}}\par
\vspace{0.2em}
{\fontsize{12pt}{16pt}\selectfont Nguyễn Thái Thông \quad | \quad Trần Hà Khánh Duy}\par
\end{center}
\vspace{0.5em}

\section{Thông tin nhóm}
\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{|p{4.2cm}|Y|}
\hline
\thead{Nội dung} & \thead{Thông tin} \\
\hline
Nhóm & Group\_07 \\
\hline
Thành viên 1 & Nguyễn Thái Thông \\
\hline
Thành viên 2 & Trần Hà Khánh Duy \\
\hline
Bài toán được giao & Segmentation ảnh X-quang y khoa, phân đoạn vùng gãy xương \\
\hline
Dataset được phân công & \code{FracAtlas/segmentation} \\
\hline
Mô hình được phân công & \textbf{U-Net++} với encoder EfficientNet-B3 pretrained ImageNet \\
\hline
\end{tabularx}
\end{table}

\section{Phân công công việc trong nhóm}
\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{|p{3.45cm}|Y|p{1.65cm}|p{2.75cm}|}
\hline
\thead{Thành viên} & \thead{Công việc phụ trách} & \thead{Tỷ lệ đóng góp} & \thead{Ghi chú} \\
\hline
Nguyễn Thái Thông & Chuẩn bị pipeline đọc dữ liệu, cấu hình mô hình U-Net++, chạy baseline resize, tune threshold/postprocess, kiểm tra metric chính và viết phần phương pháp - kết quả - phân tích. & 50\% & Phụ trách pipeline, mô hình, huấn luyện baseline và tổng hợp báo cáo. \\
\hline
Trần Hà Khánh Duy & Kiểm tra split dữ liệu, hỗ trợ chạy option no-resize, xuất hình overlay, rà file kết quả, đối chiếu các nhóm case và hoàn thiện nhận xét. & 50\% & Phụ trách kiểm tra dữ liệu, hình minh họa, no-resize và rà soát kết quả. \\
\hline
\end{tabularx}
\end{table}

\section{Mô tả đề bài và dữ liệu}
Bài toán nhóm thực hiện là \important{phân đoạn nhị phân vùng gãy xương trên ảnh X-quang}. Mỗi ảnh đi kèm một mask. Nếu ảnh thuộc nhóm \textit{fracture}, mask đánh dấu vùng gãy cần tìm; nếu ảnh thuộc nhóm \textit{normal}, mask đúng sẽ là mask rỗng. Dataset được phân công là \code{FracAtlas/segmentation}; nhóm dùng đúng các file \code{train.csv}, \code{val.csv}, \code{test.csv} có sẵn và không tự chia lại dữ liệu.

Dataset được chuẩn bị dưới dạng file nén \href{https://drive.google.com/file/d/1DeJK1z59TNTuqAwA8jnhPQHO37amdYNn/view?usp=sharing}{\code{segmentation.zip}}. Khi chạy trên Colab, nhóm copy file từ Drive về \code{/content/segmentation.zip} rồi giải nén vào \code{/content/segmentation\_extracted/segmentation}. Mục đích của bước này là tăng độ ổn định khi chạy thí nghiệm: đọc nhiều ảnh nhỏ trực tiếp từ Drive có thể chậm hoặc bị ngắt, trong khi đọc từ ổ local của Colab thường ổn định hơn.

\textbf{Cấu trúc dữ liệu sau khi giải nén:}
\begin{lstlisting}
segmentation/
|-- train.csv
|-- val.csv
|-- test.csv
|-- images/
|   |-- IMGxxxxxxx.jpg
|   `-- ...
`-- masks/
    |-- IMGxxxxxxx_mask.png
    `-- ...
\end{lstlisting}

Các file CSV chứa đường dẫn ảnh/mask và metadata, gồm các cột chính như \code{image\_id}, \code{image\_path}, \code{mask\_id}, \code{mask\_path}, \code{label\_binary}, \code{label\_3class}, \code{original\_width}, \code{original\_height}, \code{mask\_area}, \code{split} và \code{note}. Trước khi train, nhóm kiểm tra lại đường dẫn ảnh/mask trong CSV; kết quả không có dòng nào bị thiếu file hoặc lỗi đọc.

\begin{table}[H]
\centering
\scriptsize
\begin{tabularx}{\textwidth}{|p{2.55cm}|p{2.75cm}|p{2.75cm}|p{2.75cm}|p{1.55cm}|Y|}
\hline
\thead{Tập dữ liệu} & \thead{Train} & \thead{Val} & \thead{Test} & \thead{Nhãn/lớp} & \thead{Ghi chú} \\
\hline
Raw split & 2858 ảnh: 502 fracture, 2356 normal & 612 ảnh: 107 fracture, 505 normal & 613 ảnh: 108 fracture, 505 normal & 2 lớp & Split chính thức của dataset. \\
\hline
Train dùng cho baseline & 2858 ảnh sau oversampling: 1572 fracture, 1286 normal & 612 ảnh & 613 ảnh full test & 2 lớp & Oversampling chỉ áp dụng trên train, không đụng val/test. \\
\hline
No-resize & 192 ảnh train & 128 ảnh val & 160 ảnh test subset: 23 fracture, 137 normal & 2 lớp & Giữ ảnh gốc, nhưng chỉ chạy subset do giới hạn tài nguyên. \\
\hline
\end{tabularx}
\end{table}

Dữ liệu bị lệch khá rõ: trong raw train, ảnh fracture chỉ chiếm khoảng 17.6\%. Vùng gãy cũng thường rất nhỏ so với cả ảnh X-quang. Vì vậy nếu chỉ nhìn metric trung bình trên toàn bộ test, kết quả có thể bị ảnh hưởng mạnh bởi nhóm ảnh normal có mask rỗng. Trong phần kết quả, nhóm tách riêng metric trên các ảnh thật sự có mask fracture để đánh giá rõ hơn khả năng phân đoạn.

\section{Phương pháp và mô hình}
Mô hình được phân công là \important{U-Net++}; nhóm triển khai lại bằng PyTorch để huấn luyện và đánh giá trên dataset được giao. Mô hình gồm encoder pretrained để lấy đặc trưng từ ảnh X-quang và decoder U-Net++ để dựng lại bản đồ phân đoạn. Đầu ra cuối cùng là một kênh logit; sau đó nhóm dùng sigmoid và threshold để chuyển thành mask nhị phân.

\subsection*{Framework và thư viện}
\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{|p{4cm}|Y|}
\hline
\thead{Thành phần} & \thead{Vai trò trong bài làm} \\
\hline
Python, PyTorch & Xây dựng mô hình, huấn luyện, đánh giá, tính loss và metric. Môi trường thực nghiệm dùng PyTorch 2.11.0+cu128 và CUDA. \\
\hline
\code{timm} & Tạo encoder EfficientNet-B3 pretrained ImageNet dưới dạng feature extractor. \\
\hline
PIL & Đọc ảnh grayscale, đọc mask, resize/rotate/flip và augmentation cơ bản. \\
\hline
NumPy, pandas & Xử lý array, đọc CSV, tổng hợp kết quả và xuất file \code{results\_Group07.csv}. \\
\hline
SciPy & Hỗ trợ xử lý connected components trong bước postprocess mask. \\
\hline
Matplotlib & Vẽ hình minh họa gồm ảnh đầu vào, GT mask, prediction và overlay. \\
\hline
\end{tabularx}
\end{table}

\subsection*{Encoder pretrained}
Encoder sử dụng \code{efficientnet\_b3} từ thư viện \code{timm}, bật \code{pretrained=True} để dùng trọng số ImageNet. Vì ảnh X-quang là grayscale, nhóm chuyển ảnh thành 3 kênh bằng cách lặp lại kênh grayscale, sau đó chuẩn hóa theo mean/std ImageNet. Encoder được cấu hình với \code{features\_only=True} và lấy 5 mức feature map qua \code{out\_indices=(0,1,2,3,4)}. Các mức feature này có số channel lần lượt là:
\[
[24, 32, 48, 136, 384].
\]
Encoder được freeze trong epoch đầu để ổn định huấn luyện, sau đó unfreeze để fine-tune cùng decoder.

\subsection*{Decoder U-Net++}
Decoder dùng cấu trúc U-Net++ với các skip connection lồng nhau. Cách thiết kế này giúp mô hình vừa giữ được chi tiết ở tầng nông, vừa tận dụng đặc trưng ngữ nghĩa ở tầng sâu. Điều này khá phù hợp với bài toán fracture vì vùng gãy thường nhỏ và biên không rõ. Mỗi block decoder gồm convolution, GroupNorm, SiLU và Dropout2d. Nhóm cũng bật \textit{deep supervision average}, tức là lấy trung bình một số nhánh đầu ra trung gian thay vì chỉ dùng output cuối, để quá trình học của decoder ổn định hơn.

Hai cấu hình decoder được dùng:
\begin{itemize}
  \item Baseline \code{resize\_256}: decoder channels \([48, 96, 192, 384]\), trainable params khoảng 16.7M.
  \item Option \code{no\_resize\_original}: decoder channels \([16, 32, 64, 96]\) để giảm bộ nhớ khi input giữ kích thước gốc.
\end{itemize}

\subsection*{Hàm mất mát}
Loss là tổ hợp giữa Focal BCE with logits và Tversky loss:
\[
\mathcal{L} = 0.35 \times \mathcal{L}_{\text{Focal BCE}} + 0.65 \times \mathcal{L}_{\text{Tversky}}.
\]
Trong đó Focal BCE dùng \(\alpha=0.85\), \(\gamma=2.0\). Tversky loss dùng \(\alpha=0.25\), \(\beta=0.75\). Focal BCE giúp giảm ảnh hưởng của pixel dễ, còn Tversky loss phù hợp với segmentation mất cân bằng vì vùng fracture nhỏ hơn rất nhiều so với nền.

\subsection*{Optimizer, scheduler và cấu hình huấn luyện}
\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{|p{4cm}|Y|}
\hline
\thead{Thành phần} & \thead{Cấu hình} \\
\hline
Optimizer & AdamW, chia hai param group: encoder LR \(3\times10^{-5}\), decoder LR \(3.5\times10^{-4}\). \\
\hline
Weight decay & \(10^{-4}\). \\
\hline
Scheduler & CosineAnnealingLR, \code{eta\_min = DECODER\_LR * 0.05}. \\
\hline
Mixed precision & Bật AMP để giảm VRAM và tăng tốc trên GPU. \\
\hline
Gradient accumulation & Baseline dùng accumulation 2; no-resize dùng accumulation 4 do batch size 1. \\
\hline
OOM handling & Với no-resize, nhóm cho phép bỏ qua batch bị OOM để quá trình huấn luyện không dừng giữa chừng. \\
\hline
\end{tabularx}
\end{table}

\section{Tiền xử lý dữ liệu}
Pipeline đọc ảnh bằng PIL ở chế độ grayscale. Mask được chuyển thành nhị phân, trong đó pixel thuộc vùng fracture là 1 và nền là 0. Vì encoder pretrained trên ImageNet nhận ảnh 3 kênh, nhóm lặp lại kênh grayscale thành 3 kênh rồi chuẩn hóa theo mean \([0.485, 0.456, 0.406]\) và std \([0.229, 0.224, 0.225]\) của ImageNet.

Với baseline \code{resize\_256}, ảnh và mask được đưa về kích thước \(256 \times 256\). Ảnh dùng bilinear interpolation; mask dùng nearest-neighbor để tránh sinh ra các giá trị mask trung gian. Với mode \code{no\_resize\_original}, nhóm giữ nguyên kích thước ảnh gốc: \important{không resize vuông, không padding và không cap cạnh dài}. Ở đây, \textit{cap cạnh dài} nghĩa là giới hạn cạnh lớn nhất của ảnh về một ngưỡng cố định, ví dụ ảnh quá lớn thì thu nhỏ theo đúng tỉ lệ để cạnh dài không vượt quá 1024 hoặc 1536 pixel. Nhóm không dùng bước này, nên ảnh đi vào model đúng theo kích thước gốc; đổi lại batch size phải đặt bằng 1 vì mỗi ảnh có một kích thước khác nhau.

Augmentation chỉ áp dụng cho train và giữ ở mức vừa phải:
\begin{itemize}
  \item Horizontal flip với xác suất 0.5.
  \item Vertical flip nhẹ với xác suất 0.15.
  \item Rotate trong khoảng \(-7^\circ\) đến \(7^\circ\) với xác suất 0.25.
  \item Autocontrast với xác suất 0.25.
  \item Chỉnh contrast trong khoảng 0.80-1.30 với xác suất 0.40.
  \item Chỉnh brightness trong khoảng 0.88-1.14 với xác suất 0.35.
\end{itemize}

\subsection*{Train, chọn threshold và hậu xử lý}
Nhóm thực nghiệm với hai mode: \code{no\_resize\_original} và \code{resize\_256}. No-resize được dùng để kiểm tra hướng giữ ảnh gốc, còn resize 256 là baseline chính. Với mỗi mode, nhóm dùng pipeline dữ liệu và cấu hình mô hình riêng, chọn checkpoint tốt nhất theo balanced validation score. Threshold được chọn trên \code{val.csv}, sau đó mới đánh giá trên \code{test.csv} hoặc test subset.

Balanced validation score trong giai đoạn monitor được tính theo hướng ưu tiên ảnh fracture nhưng vẫn kiểm tra ảnh normal:
\[
\text{score} = 0.70 \times \text{Dice}_{\text{positive}} + 0.30 \times \text{NormalEmptyRate}.
\]
Điểm này chỉ dùng để chọn checkpoint/threshold trên validation, không thay cho metric test cuối.

Sau khi model dự đoán mask, nhóm có thêm bước hậu xử lý. Bước này lọc connected components quá nhỏ và kiểm soát tổng diện tích mask dự đoán. Riêng baseline resize dùng thêm image-level gate để giảm false positive trên ảnh normal. Bộ tham số tốt nhất trên validation resize là \code{seg\_threshold=0.08}, \code{gate\_max\_prob\_threshold=0.55}, \code{gate\_min\_soft\_area\_ratio=0.0005}. Với no-resize, nhóm tắt gate này để hạn chế nguy cơ loại bỏ nhầm các vùng fracture nhỏ.

\begin{table}[H]
\centering
\scriptsize
\begin{tabularx}{\textwidth}{|p{3.55cm}|p{1.45cm}|p{1.8cm}|p{1.95cm}|Y|}
\hline
\thead{Mode} & \thead{Epoch} & \thead{Batch} & \thead{Threshold} & \thead{Ghi chú thực nghiệm} \\
\hline
\code{resize\_256} & 40 & 12, accum 2 & 0.08 + gate & Checkpoint tốt nhất ở epoch 36, điểm theo dõi 0.459421; đánh giá trên toàn bộ 613 ảnh test. \\
\hline
\code{no\_resize\_original} & 6 & 1, accum 4 & 0.03, không gate & Checkpoint tốt nhất ở epoch 1; đánh giá trên subset 160 ảnh; một số batch OOM được bỏ qua ở các epoch sau. \\
\hline
\end{tabularx}
\end{table}

\section{Kết quả baseline resize}
Baseline resize là phần bắt buộc của bài làm. Nhóm huấn luyện mode này trong 40 epoch và đánh giá trên toàn bộ 613 ảnh trong \code{test.csv}. Các metric chính gồm Dice, IoU, Precision và Recall.

\begin{table}[H]
\centering
\scriptsize
\begin{tabularx}{\textwidth}{|p{3.1cm}|p{2.3cm}|C{1.25cm}|C{1.25cm}|C{1.35cm}|C{1.25cm}|Y|}
\hline
\thead{Dataset} & \thead{Model} & \thead{Dice} & \thead{IoU} & \thead{Precision} & \thead{Recall} & \thead{Ghi chú} \\
\hline
FracAtlas full test & U-Net++ & 0.369738 & 0.349318 & 0.368879 & 0.381903 & 613 ảnh test, gồm cả ảnh normal mask rỗng. \\
\hline
Ảnh có mask fracture thật & U-Net++ & 0.496755 & 0.380850 & 0.491880 & 0.565802 & 108 ảnh fracture trong test. Đây là metric quan trọng để đánh giá khả năng phân đoạn vùng gãy. \\
\hline
\end{tabularx}
\end{table}

Theo phân loại case trên test, baseline có 36 \textit{good case}, 48 \textit{medium case}, 18 \textit{hard/error case}, 6 \textit{fracture false negative}, 173 \textit{normal true negative} và 332 \textit{normal false positive}. Tỷ lệ false positive trên ảnh normal là 332/505 = 0.657426. Điều này cho thấy mô hình bắt được nhiều vùng fracture thật hơn no-resize, nhưng vẫn còn dự đoán nhiễu trên ảnh normal.

\section{Option no-resize / giữ kích thước ảnh gốc}
Mode \code{no\_resize\_original} được dùng để kiểm tra hướng giữ nguyên kích thước ảnh gốc. Nhóm lựa chọn \important{không resize, không padding và không cap cạnh dài}. Nói cách khác, ảnh và mask được đưa vào model đúng như kích thước ban đầu của từng ảnh. \textit{Cap cạnh dài} là cách thường dùng để giảm tải bộ nhớ: nếu ảnh quá lớn, ta thu nhỏ toàn bộ ảnh theo cùng một tỉ lệ sao cho cạnh dài nhất không vượt quá một ngưỡng đặt trước. Vì nhóm không cap cạnh dài, kích thước input thay đổi khá nhiều giữa các ảnh, batch size phải để bằng 1 và decoder phải giảm kênh xuống \([16,32,64,96]\) cho vừa giới hạn Colab free.

Thử nghiệm này cho thấy pipeline ảnh gốc có thể vận hành, nhưng chưa thể xem là cấu hình tối ưu. Khi giữ ảnh gốc, số pixel nền tăng lên rất nhiều trong khi vùng fracture vẫn nhỏ. Bộ nhớ GPU cũng không chỉ phụ thuộc vào ảnh đầu vào, mà còn phụ thuộc vào activation của encoder, decoder và các skip connection của U-Net++. Vì vậy no-resize dễ OOM hơn baseline resize. Nhóm áp dụng AMP, gradient accumulation, decoder checkpoint và cơ chế bỏ qua batch OOM để quá trình huấn luyện không bị dừng giữa chừng. Dù vậy, trong quá trình huấn luyện no-resize vẫn có 18 batch bị bỏ qua do OOM ở các epoch sau, nên giới hạn tài nguyên có ảnh hưởng trực tiếp đến lượng dữ liệu mô hình học được.

Thời gian chạy của no-resize cũng không ổn định: epoch đầu khoảng 236.5 giây, epoch 2 khoảng 250.1 giây, còn các epoch sau khoảng 65-68 giây nhưng vẫn có batch OOM bị bỏ qua. Sự khác biệt này đến từ kích thước ảnh không đồng nhất, cache/data loading và số batch thực tế được xử lý. Vì vậy nhóm giới hạn thử nghiệm ở 192 ảnh train, 128 ảnh validation và 160 ảnh test subset để phù hợp với Colab free. Với GPU VRAM lớn hơn, hướng này có thể được đánh giá đầy đủ hơn bằng cách tăng số mẫu, tăng decoder channels, train lâu hơn, hoặc dùng tiling/padding hợp lý.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{|p{3.0cm}|p{3.35cm}|p{3.35cm}|Y|}
\hline
\thead{Cấu hình} & \thead{Input size/cách xử lý} & \thead{Metric chính} & \thead{Nhận xét} \\
\hline
Baseline resize & Resize vuông về \(256 \times 256\), đánh giá trên đủ 613 ảnh test. & Dice full test 0.369738; Dice positive 0.496755. & Ổn định hơn, huấn luyện được 40 epoch, nhưng false positive trên normal còn cao. \\
\hline
No-resize original & Giữ nguyên kích thước ảnh gốc, batch size 1, subset test 160 ảnh. & Dice all 0.856250; IoU all 0.856250; Dice positive 0.000000. & Pipeline ảnh gốc vận hành được nhưng chưa phân đoạn được ảnh fracture; metric all cao do nhiều ảnh normal được dự đoán rỗng đúng. \\
\hline
\end{tabularx}
\end{table}

Subset no-resize gồm 137 ảnh normal và 23 ảnh fracture. Kết quả có 137 normal true negative và 23 fracture false negative. Nói cách khác, tất cả ảnh fracture trong subset đều bị dự đoán rỗng. Vì vậy Dice/IoU/Precision/Recall trên nhóm ảnh có mask thật đều bằng 0. Con số \code{Dice all = 0.856250} nhìn qua có vẻ cao, nhưng không thể xem là kết quả tốt; nó chủ yếu đến từ việc mô hình dự đoán rỗng đúng trên nhiều ảnh normal.

\subsection*{CSV kết quả và khả năng truy vết}
Nhóm tổng hợp kết quả vào file \code{results\_Group07.csv} với 773 dòng: 160 dòng cho no-resize và 613 dòng cho baseline resize. Các cột chính gồm \code{group\_id}, \code{student\_1}, \code{student\_2}, \code{task\_id}, \code{dataset}, \code{model}, \code{image\_id}, \code{split}, \code{original\_width}, \code{original\_height}, \code{input\_width}, \code{input\_height}, \code{preprocess\_mode}, \code{gt\_mask\_area}, \code{pred\_mask\_area}, \code{dice}, \code{iou}, \code{precision}, \code{recall}, \code{case\_type} và các cột khai báo AI.

Về độ bao phủ dữ liệu, baseline resize có đủ 613/613 image id trong test.csv. No-resize chỉ có 160/613 image id vì đây là subset chạy thử để giảm tải tài nguyên. 


\section{Hình minh họa kết quả}
Các hình minh họa dưới đây được lưu trong thư mục \code{figures}. Mỗi hình gồm ảnh đầu vào, ground truth mask, predicted mask và overlay, giúp quan sát trực quan các trường hợp dự đoán đúng hoặc sai thay vì chỉ dựa vào metric.

\begin{figure}[H]
\centering
\imgmaybe{figures/overlay_resize_256_good_case_IMG0001411.jpg.png}{overlay\_resize\_256\_good\_case\_IMG0001411.jpg.png}
\caption{Baseline resize - good case. Mask dự đoán khớp khá tốt với vùng fracture thật.}
\end{figure}

\begin{figure}[H]
\centering
\imgmaybe{figures/overlay_resize_256_medium_case_IMG0002502.jpg.png}{overlay\_resize\_256\_medium\_case\_IMG0002502.jpg.png}
\caption{Baseline resize - medium case. Mô hình bắt được vùng fracture nhưng dự đoán còn rộng hơn GT.}
\end{figure}

\begin{figure}[H]
\centering
\imgmaybe{figures/overlay_resize_256_fracture_false_negative_IMG0002840.jpg.png}{overlay\_resize\_256\_fracture\_false\_negative\_IMG0002840.jpg.png}
\caption{Baseline resize - fracture false negative. Đây là trường hợp vùng fracture bị bỏ sót.}
\end{figure}

\begin{figure}[H]
\centering
\imgmaybe{figures/overlay_resize_256_normal_false_positive_IMG0002219.jpg.png}{overlay\_resize\_256\_normal\_false\_positive\_IMG0002219.jpg.png}
\caption{Baseline resize - normal false positive. Ảnh normal nhưng mô hình vẫn sinh mask dự đoán.}
\end{figure}

\begin{figure}[H]
\centering
\imgmaybe{figures/overlay_resize_256_normal_true_negative_IMG0001807.jpg.png}{overlay\_resize\_256\_normal\_true\_negative\_IMG0001807.jpg.png}
\caption{Baseline resize - normal true negative. Ảnh normal được dự đoán rỗng đúng.}
\end{figure}

\begin{figure}[H]
\centering
\imgmaybe{figures/overlay_no_resize_original_fracture_false_negative_IMG0000188.jpg.png}{overlay\_no\_resize\_original\_fracture\_false\_negative\_IMG0000188.jpg.png}
\caption{No-resize - fracture false negative. Mode giữ ảnh gốc vận hành được nhưng chưa phát hiện được vùng fracture trong trường hợp này.}
\end{figure}

\section{Phân tích và nhận xét}
Baseline resize cho thấy mô hình đã học được tín hiệu fracture ở mức nhất định. Dice positive đạt 0.496755 và Recall positive đạt 0.565802, tức là trên nhóm ảnh có mask thật, mô hình bắt được hơn một nửa vùng fracture theo trung bình. Điểm yếu rõ nhất là Precision positive 0.491880 và số normal false positive còn cao, nghĩa là mô hình vẫn tạo nhiều mask dự đoán không cần thiết trên ảnh normal.

Theo nhóm, false positive có thể đến từ vài nguyên nhân. Vùng gãy thường nhỏ và biên không rõ, nên cạnh xương hoặc vùng tương phản mạnh dễ bị model nhầm là tổn thương. Ngoài ra, nhóm có oversampling ảnh fracture để model gặp nhiều positive hơn trong train; cách này giúp tăng cơ hội học fracture nhưng cũng có thể làm model nhạy quá mức với các chi tiết giống fracture. Cuối cùng, threshold 0.08 của baseline khá thấp để giữ lại fracture nhỏ, nên một số vùng nhiễu cũng bị giữ lại.

No-resize vận hành được về mặt kỹ thuật, nhưng chất lượng chưa đạt. Khi giữ ảnh gốc, vùng fracture chiếm tỷ lệ nhỏ hơn trong toàn ảnh do phần nền lớn hơn. Cấu hình no-resize lại phải giảm decoder, dùng batch size 1, train ít epoch hơn và chỉ chạy subset để tránh OOM trên Colab free. Val score đứng ở 0.30000 trong nhiều epoch; trên test subset, toàn bộ ảnh fracture bị false negative. Như vậy, ở cấu hình hiện tại, mô hình có xu hướng dự đoán rỗng trên ảnh normal nhưng chưa học được đặc trưng của vùng fracture.

Dù vậy, no-resize vẫn là hướng đáng tiếp tục thử nghiệm vì nó giữ được hình dạng và tỉ lệ ảnh gốc. Nếu có tài nguyên tốt hơn, có thể tăng số mẫu, train lâu hơn, tăng decoder channels, dùng tiling hoặc padding theo batch để giảm OOM, rồi tune riêng threshold/postprocess cho no-resize. Với baseline resize, hướng cải thiện quan trọng nhất là giảm false positive, chẳng hạn bằng hard negative mining hoặc một image-level classifier/gate tốt hơn.

\section{Công cụ AI hỗ trợ và cam kết}
\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{|p{5cm}|Y|}
\hline
\thead{Nội dung} & \thead{Khai báo của nhóm} \\
\hline
Có sử dụng AI không? & Có. \\
\hline
Công cụ AI đã dùng & ChatGPT. \\
\hline
Phạm vi sử dụng & Hỗ trợ sửa lỗi code, gợi ý cấu trúc báo cáo, diễn giải metric và rà lại các điểm dễ gây hiểu nhầm. Không dùng AI để bịa metric hoặc tạo hình minh họa giả. \\
\hline
Kết quả có truy vết từ quá trình thực nghiệm không? & Có. Metric, CSV và hình overlay đều được lưu từ quá trình chạy thực nghiệm trong \code{fracatlas\_unetpp\_outputs}. \\
\hline
Các thành viên có thể giải thích bài nộp không? & Có. Các thành viên nắm được pipeline dữ liệu, mô hình, loss, threshold, postprocess và hạn chế của no-resize. \\
\hline
\end{tabularx}
\end{table}

\section{Kết luận}
Nhóm đã hoàn thành baseline resize cho bài toán segmentation ảnh X-quang với dataset \code{FracAtlas/segmentation} và mô hình \textbf{U-Net++} được phân công. Với encoder EfficientNet-B3 pretrained ImageNet, kết quả đạt Dice 0.369738 trên full test và Dice 0.496755 trên nhóm ảnh có mask fracture thật.

Option no-resize đã được triển khai theo hướng giữ kích thước ảnh gốc, không resize và không cap cạnh dài. Cap cạnh dài ở đây là thao tác thu nhỏ ảnh theo tỉ lệ khi cạnh lớn nhất vượt quá một ngưỡng cố định; nhóm không dùng thao tác này để kiểm tra đúng pipeline ảnh gốc. Tuy nhiên do giới hạn tài nguyên Colab free, mode này chỉ chạy subset và phải dùng cấu hình nhẹ. Kết quả no-resize hiện tại chưa đạt yêu cầu trên ảnh fracture vì toàn bộ 23 ảnh fracture trong subset bị false negative. Bài học chính là với dữ liệu có nhiều mask rỗng, cần đọc metric theo từng nhóm ảnh, đặc biệt là metric trên ảnh có tổn thương thật, thay vì chỉ nhìn Dice trung bình toàn test.

\section{Checklist trước khi nộp}
\begin{itemize}
  \item \code{report\_Group07.pdf}: xuất từ file \code{report\_Group07.tex} bằng XeLaTeX.
  \item \code{results\_Group07.csv}: file kết quả tổng hợp, không có cột index thừa, metric khớp với quá trình đánh giá.
  \item \code{notebook\_link.txt}: chứa link Colab đã chạy đầy đủ và bật quyền xem.
  \item \code{PhanCong\_Group07.pdf} hoặc mục phân công trong báo cáo: đã có mục phân công 50\%/50\%.
  \item \code{figures/}: chứa hình overlay được tạo từ quá trình chạy thật.
  \item File nộp Moodle cuối cùng cần đặt đúng tên \code{Group\_07.zip} nếu nộp dạng ZIP.
\end{itemize}

\end{document}

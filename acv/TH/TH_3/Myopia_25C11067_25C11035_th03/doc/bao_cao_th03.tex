\documentclass[12pt,a4paper]{article}
\usepackage{fontspec}
\setmainfont{TeX Gyre Termes}
\usepackage[a4paper,margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{float}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{enumitem}
\hypersetup{colorlinks=true, linkcolor=blue, urlcolor=blue}
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\renewcommand{\arraystretch}{1.25}
\setlength{\headheight}{15pt}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}

\begin{document}

\begin{titlepage}
    \thispagestyle{empty}
    \centering

    \vspace*{1.5cm}
    \IfFileExists{../../../../../assets/rsz_logo-khtn.png}
    {\includegraphics[width=3.2cm]{../../../../../assets/rsz_logo-khtn.png}\par}
    {\vspace{3.2cm}\par}
    \vspace{0.8cm}

    {\Large\bfseries ĐẠI HỌC QUỐC GIA THÀNH PHỐ HỒ CHÍ MINH\par}
    \vspace{0.2cm}
    {\Large\bfseries TRƯỜNG ĐẠI HỌC KHOA HỌC TỰ NHIÊN\par}
    \vspace{0.15cm}
    {\large KHOA CÔNG NGHỆ THÔNG TIN\par}

    \vspace{2.0cm}
    {\Large\bfseries BÁO CÁO THỰC HÀNH 03\par}
    \vspace{0.4cm}
    {\large\bfseries Phát hiện đối tượng bằng YOLO, DETR và Faster R-CNN\par}

    \vspace{1.8cm}
    \begin{tabular}{>{\bfseries}l l}
        Nhóm: & Myopia \\
        MSHV 1: & 25C11067 - Nguyễn Thái Thông \\
        MSHV 2: & 25C11035 - Trần Hạ Khánh Duy
    \end{tabular}

    \vfill
    {\large TP. Hồ Chí Minh, 2026\par}
\end{titlepage}
\clearpage
\pagenumbering{arabic}

\section{Mục tiêu}

Bài thực hành 03 tập trung vào bài toán phát hiện đối tượng (object detection) bằng YOLO, DETR và Faster R-CNN. Nhóm chọn trên bộ dữ liệu ảnh bàn cờ có các quân cờ ở nhiều trạng thái sắp xếp khác nhau và xây dựng một pipeline hoàn chỉnh gồm chuẩn hóa dữ liệu, huấn luyện ba mô hình phát hiện đối tượng, xuất ảnh dự đoán và đánh giá kết quả bằng các biểu đồ trực quan.

Các mục tiêu chính của bài gồm:
\begin{itemize}[leftmargin=1.2cm]
    \item Tìm hiểu cấu trúc dữ liệu nhãn cho bài toán detection và chuyển đổi giữa các định dạng annotation khác nhau.
    \item Cài đặt, huấn luyện và so sánh ba mô hình YOLO, DETR và Faster R-CNN trên cùng một bộ ảnh.
    \item Xuất ảnh prediction, confusion matrix và đường cong đánh giá để phục vụ báo cáo.
    \item Quan sát ảnh hưởng của dữ liệu mất cân bằng lớp, số lượng object trên ảnh và đặc điểm từng mô hình đến chất lượng dự đoán.
\end{itemize}

Bộ dữ liệu sử dụng trong thực nghiệm gồm 693 ảnh, chia thành 606 ảnh train, 58 ảnh validation và 29 ảnh test. Tổng số bounding box là 7{,}086. Dữ liệu có 13 lớp quân cờ, trong đó một số lớp xuất hiện rất ít như \texttt{bishop}, làm cho bài toán có tính mất cân bằng rõ rệt.

\section{Phân công đóng góp}

\begin{longtable}{p{0.24\textwidth}p{0.68\textwidth}}
\toprule
\textbf{Thành viên} & \textbf{Nội dung đóng góp} \\
\midrule
Nguyễn Thái Thông & Chuẩn bị dataset, xây dựng pipeline tiền xử lý; triển khai và đánh giá YOLO; thực hiện trực quan hóa kết quả dự đoán; viết phần mô tả dữ liệu và phương pháp. \\
Trần Hạ Khánh Duy & Kiểm tra annotation và chuyển đổi định dạng dữ liệu; triển khai và đánh giá DETR, Faster R-CNN; thực hiện so sánh thực nghiệm và viết phần kết quả, nhận xét, kết luận. \\
\bottomrule
\end{longtable}

\section{Bảng tự đánh giá}

\begin{longtable}{p{0.24\textwidth}p{0.18\textwidth}p{0.48\textwidth}}
\toprule
\textbf{Thành viên} & \textbf{Mức đóng góp} & \textbf{Ghi chú} \\
\midrule
Nguyễn Thái Thông & 100\% & Hoàn thành phần được phân công, phối hợp kiểm tra pipeline và hình minh chứng. \\
Trần Hạ Khánh Duy & 100\% & Hoàn thành phần được phân công, phối hợp rà soát kết quả huấn luyện và báo cáo. \\
\bottomrule
\end{longtable}

\section{Cơ sở lý thuyết và cách cài đặt}

\subsection{Tổng quan dữ liệu và tiền xử lý}

Bộ dữ liệu được chọn là \textbf{Chess Pieces 416x416 Augmented} trên Roboflow Public Dataset, thuộc bài toán phát hiện quân cờ trên ảnh bàn cờ. Dataset gồm các ảnh kích thước 416$\times$416, đã được chia sẵn theo các split \texttt{train}, \texttt{valid} và \texttt{test}; mỗi ảnh có annotation bounding box cho các quân cờ như pawn, rook, bishop, knight, queen và king theo hai màu đen/trắng. Đây là tập dữ liệu phù hợp với bài thực hành vì ảnh có nền bàn cờ tương đối rõ, nhưng vẫn có các tình huống khó như quân cờ nhỏ, nằm sát biên ảnh, bị che khuất một phần hoặc nhiều quân xuất hiện dày đặc.

Liên kết truy xuất và tải dữ liệu:
\begin{itemize}[leftmargin=1.2cm]
    \item Link gốc Roboflow: \href{https://public.roboflow.com/object-detection/chess-full/24/download}{https://public.roboflow.com/object-detection/chess-full/24/download}
    \item Link reup Google Drive: \href{https://drive.google.com/file/d/1sww39WoPZ6ICktbJwbSsHTxOQyE7R5hH/view?usp=sharing}{https://drive.google.com/file/d/1sww39WoPZ6ICktbJwbSsHTxOQyE7R5hH}
\end{itemize}

Dữ liệu sau khi tải được lưu theo ba split \texttt{train}, \texttt{valid} và \texttt{test}. Mỗi ảnh đi kèm các nhãn bounding box tuyệt đối theo dạng \texttt{x1,y1,x2,y2,class\_id}. Khi huấn luyện, nhóm chuyển dữ liệu sang các định dạng phù hợp với từng mô hình:
\begin{itemize}[leftmargin=1.2cm]
    \item YOLO nhận nhãn theo định dạng chuẩn \texttt{class cx cy w h} và được sinh ra từ file \texttt{data.yaml}.
    \item DETR sử dụng \texttt{DetrImageProcessor} để encode ảnh và annotation sang format mà Hugging Face dùng cho mô hình transformer.
    \item Faster R-CNN dùng trực tiếp bounding box tuyệt đối và label id tăng từ 1 để chừa lớp nền.
\end{itemize}

Trên notebook, nhóm cũng xây dựng các hàm tiện ích để đọc ảnh, hiển thị ảnh, tạo grid minh họa và thống kê dữ liệu. Các hình thống kê cơ bản của bộ dữ liệu gồm ảnh mẫu, phân bố số lượng ảnh theo split và phân bố lớp quân cờ.

Các ảnh mẫu trong bộ dữ liệu cho thấy quân cờ thường xuất hiện trên nền bàn cờ khá đều màu, nhưng có thể bị che khuất một phần, chồng lấn hoặc xuất hiện với nhiều góc nhìn khác nhau. Đây là lý do khiến bài toán detection trên ảnh bàn cờ không hoàn toàn đơn giản dù số lớp chỉ ở mức trung bình.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/dataset_samples_grid.png}
    \caption{Một số ảnh mẫu của bộ dữ liệu.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.86\textwidth]{figures/sample_ground_truth.png}
    \caption{Một ảnh ground truth tiêu biểu sau khi gắn bounding box.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.78\textwidth]{figures/split_distribution.png}
    \caption{Phân bố số ảnh và số bounding box theo từng split.}
\end{figure}

Biểu đồ split cho thấy tập train chiếm phần lớn dữ liệu với 606 ảnh và hơn 6{,}000 bounding box, trong khi validation có 58 ảnh và test có 29 ảnh. Số box ở validation và test chỉ ở mức vài trăm, vì vậy từng ảnh test có ảnh hưởng khá rõ đến confusion matrix và các nhận xét định lượng.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.86\textwidth]{figures/class_distribution.png}
    \caption{Phân bố số bounding box theo lớp.}
\end{figure}

Từ biểu đồ phân bố lớp, có thể thấy dữ liệu bị lệch mạnh. Hai lớp pawn chiếm ưu thế rõ rệt: \texttt{black-pawn} khoảng 1{,}650 box và \texttt{white-pawn} khoảng 1{,}590 box. Các lớp còn lại chủ yếu nằm trong khoảng 200--500 box, ví dụ \texttt{black-queen} chỉ khoảng 200 box, còn lớp \texttt{bishop} không có box nào. Đây là nguyên nhân quan trọng khiến các mô hình học khó đồng đều trên toàn bộ lớp, đặc biệt với lớp hiếm hoặc lớp vắng mẫu.

\subsection{YOLO}

YOLO là mô hình một giai đoạn (one-stage detector), dự đoán trực tiếp vị trí và nhãn đối tượng trên ảnh đầu vào. Ưu điểm nổi bật của YOLO là tốc độ huấn luyện và suy luận nhanh, phù hợp với các bài toán cần phản hồi gần thời gian thực.

Trong pipeline của bài thực hành, nhóm dùng \texttt{yolov8n.pt} làm backbone pretrain và fine-tune trên dữ liệu cờ vua. Khi huấn luyện, ảnh được resize về cùng kích thước đầu vào, còn nhãn được chuyển sang định dạng YOLO. Việc suy luận dùng ngưỡng confidence thấp hơn để giữ lại nhiều dự đoán hữu ích hơn khi vẽ báo cáo.

\subsection{DETR}

DETR (Detection Transformer) là mô hình detection dựa trên transformer, xem bài toán phát hiện đối tượng như một bài toán dự đoán tập các object không trùng lặp. Khác với YOLO, DETR không phụ thuộc mạnh vào anchor hay các bước hậu xử lý thủ công như NMS trong pipeline cổ điển.

Trong notebook, nhóm dùng \texttt{DetrForObjectDetection} và \texttt{DetrImageProcessor} của Hugging Face để fine-tune trực tiếp trên tập ảnh bàn cờ. Do DETR khá nhạy với learning rate lớn và mixed precision trên tập dữ liệu nhỏ, quá trình train được cấu hình thận trọng hơn với \texttt{fp16=False}, learning rate thấp và gradient clipping.

\subsection{Faster R-CNN}

Faster R-CNN là mô hình hai giai đoạn (two-stage detector) gồm bước đề xuất vùng quan tâm và bước phân lớp chi tiết. So với DETR, Faster R-CNN thường ổn định hơn trong thực nghiệm nhỏ, đặc biệt khi số lớp không quá nhiều.

Nhóm sử dụng backbone pretrain của TorchVision và thay đầu ra classifier cho đúng số lớp quân cờ. Dữ liệu được đưa vào \texttt{DataLoader} theo kiểu batch detection, trong đó mỗi ảnh có số bounding box khác nhau.

\subsection{Đánh giá và xuất kết quả}

Sau khi huấn luyện, nhóm xuất ra bốn nhóm kết quả:
\begin{itemize}[leftmargin=1.2cm]
    \item Biểu đồ train/eval loss hoặc metric curve.
    \item Confusion matrix trên tập test.
    \item Ảnh prediction mẫu cho 5 ảnh test đã chọn.
    \item Các panel so sánh giữa YOLO, DETR và Faster R-CNN trên cùng một bộ ảnh test.
\end{itemize}

Mục tiêu của phần đánh giá không chỉ là nhìn số liệu, mà còn quan sát trực quan xem mô hình nào giữ được nhiều quân cờ hơn, mô hình nào nhầm lớp, và mô hình nào phù hợp hơn với ảnh có ít object hoặc ảnh có quân cờ bị che khuất.

\section{Quy trình thực nghiệm}

Quy trình thực nghiệm của bài được thực hiện theo các bước:
\begin{enumerate}[leftmargin=1.2cm]
    \item Đọc file nhãn gốc và thống kê số lượng ảnh, số box, số lớp.
    \item Hiển thị một số ảnh mẫu và ảnh ground truth để kiểm tra chất lượng annotation.
    \item Huấn luyện YOLO, DETR và Faster R-CNN 40 epochs mỗi mô hình trên cùng bộ dữ liệu.
    \item Dùng cùng một tập test image để xuất prediction của ba mô hình.
    \item Lưu confusion matrix, evaluation curve và các ảnh so sánh trực quan;
    \item Tổng hợp nhận xét về ưu và nhược điểm của từng mô hình.
\end{enumerate}

Nhóm chọn 5 ảnh test tiêu biểu để so sánh giữa ba mô hình. Các ảnh này được lấy từ cùng một danh sách test image nhằm đảm bảo tính công bằng khi so sánh. Ngoài ra, hai ảnh khó nhất còn được tách riêng để tạo ảnh so sánh lỗi, giúp nhìn rõ hơn trường hợp nào mô hình dự đoán thiếu hoặc dự đoán nhầm lớp.

Link minh chứng thực nghiệm trên Kaggle: \href{https://www.kaggle.com/code/duytranhakhanh/acv-th03-object-detection-chess-pieces?scriptVersionId=329920509}{https://www.kaggle.com/code/duytranhakhanh/acv-th03-object-detection-chess-pieces?scriptVersionId=329920509}.

\section{Kết quả và nhận xét}

\subsection{Kết quả huấn luyện và đánh giá mô hình}

Toàn bộ đánh giá định lượng trong bài đều được tính trên cùng tập \texttt{test}. Confusion matrix, đường cong đánh giá và các ảnh prediction đều dùng chung bộ test image, cùng ngưỡng confidence trong quá trình hiển thị, nên có thể so sánh trực tiếp giữa ba mô hình.

\subsubsection{Diễn biến huấn luyện}

Đường cong huấn luyện của YOLO cho thấy mô hình hội tụ rõ. \texttt{train/box\_loss} giảm từ khoảng 1.7 xuống gần 0.72, \texttt{train/cls\_loss} giảm từ hơn 4.0 xuống khoảng 0.51, còn \texttt{train/dfl\_loss} giảm từ khoảng 1.19 xuống 0.85. Trên validation, \texttt{val/box\_loss} giảm về khoảng 0.76, \texttt{val/cls\_loss} còn khoảng 0.45 và \texttt{val/dfl\_loss} còn khoảng 0.87. Các metric validation tăng nhanh sau vài epoch đầu và ổn định ở mức cao: precision và recall xấp xỉ 0.98--0.99, \texttt{mAP50} gần 0.99, còn \texttt{mAP50-95} đạt khoảng 0.80 ở cuối quá trình train.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/yolo_training_curve.png}
    \caption{Đường cong huấn luyện và validation của YOLO.}
\end{figure}

Với DETR, train loss giảm từ khoảng 3.1 ở giai đoạn đầu xuống khoảng 0.34 ở cuối quá trình fine-tune. Eval loss cũng giảm nhanh từ khoảng 1.4 xuống gần 0.33 trong các lần đánh giá, cho thấy mô hình học được biểu diễn phù hợp hơn với tập cờ vua. Đường train loss vẫn dao động nhiều hơn YOLO ở các bước đầu, nhưng càng về cuối càng phẳng và ổn định hơn, phản ánh quá trình fine-tune transformer trên tập dữ liệu nhỏ đã bắt đầu hội tụ.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.86\textwidth]{figures/detr_training_curve.png}
    \caption{Đường cong huấn luyện của DETR.}
\end{figure}

\subsubsection{Confusion matrix}

Ở bảng confusion matrix, cả ba mô hình đều giữ được xu hướng đường chéo rõ. YOLO dự đoán đúng 86 mẫu \texttt{black-pawn}, 87 mẫu \texttt{white-pawn}, 24 mẫu \texttt{black-rook}, 25 mẫu \texttt{white-bishop}, 25 mẫu \texttt{white-knight} và 19 mẫu \texttt{white-rook}. Các lỗi của YOLO chủ yếu là các ô nhỏ ngoài đường chéo: 2 mẫu \texttt{black-rook}, 1 mẫu \texttt{white-pawn} và 1 mẫu \texttt{white-rook} bị đưa về \texttt{background}; ngoài ra có vài false positive nền bị gán thành \texttt{black-pawn}, \texttt{black-bishop}, \texttt{white-bishop}, \texttt{white-knight} hoặc \texttt{white-rook}.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/yolo_confusion_matrix.png}
        \small YOLO
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/detr_confusion_matrix.png}
        \small DETR
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/faster_rcnn_confusion_matrix.png}
        \small Faster R-CNN
    \end{minipage}
    \caption{So sánh confusion matrix của ba mô hình trên cùng tập test.}
\end{figure}

DETR cũng nhận tốt hai lớp pawn với 86 mẫu \texttt{black-pawn} và 88 mẫu \texttt{white-pawn} đúng. Một số lỗi của DETR nằm ở hàng/cột \texttt{background}: 2 mẫu \texttt{black-bishop}, 3 mẫu \texttt{black-queen}, 1 mẫu \texttt{black-rook} và 2 mẫu \texttt{white-bishop} bị bỏ sót; đồng thời nền bị gán thành \texttt{black-bishop}, \texttt{black-pawn}, \texttt{white-bishop}, \texttt{white-pawn} hoặc \texttt{white-rook} với số lượng rất nhỏ. Faster R-CNN là confusion matrix sạch nhất trong ba mô hình: \texttt{black-pawn} đúng 86, \texttt{white-pawn} đúng 88, \texttt{black-rook} đúng 26 và \texttt{white-rook} đúng 20; lỗi nổi bật nhất là 2 mẫu \texttt{black-knight} bị gán sang \texttt{black-rook}.

\subsubsection{Prediction theo cùng ảnh test}

Nhóm dùng cùng 5 ảnh test tiêu biểu để nhìn prediction của cả ba mô hình theo từng case. Các panel dưới đây lần lượt hiển thị ảnh gốc, YOLO, DETR và mô hình thứ ba là Faster R-CNN. Cách trình bày này giúp nhận ra rất rõ sự khác nhau giữa các model trong cùng điều kiện đầu vào.

Case 1 là ảnh gần như trống, chỉ có một phần quân cờ bị cắt ở mép dưới ảnh. Cả YOLO, DETR và Faster R-CNN đều không vẽ bounding box nào. Kết quả này cho thấy các mô hình không tạo false positive trên vùng bàn cờ trống, nhưng đồng thời cũng bỏ qua object bị cắt quá mạnh ở biên ảnh.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/comparison_case_1.png}
    \caption{So sánh prediction trên test case 1.}
\end{figure}

Case 2 có một quân đen ở giữa bàn cờ. YOLO phát hiện đúng vị trí nhưng gán nhãn \texttt{black-king} với confidence 0.83, trong khi Faster R-CNN dự đoán \texttt{black-queen} với confidence 1.00. DETR không tạo prediction trong case này. Đây là ví dụ cho thấy khác biệt giữa các mô hình ở các quân đen có hình dạng gần nhau: YOLO vẫn bắt được object nhưng có khả năng nhầm lớp, còn DETR bỏ sót ở ngưỡng hiển thị hiện tại.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/comparison_case_2.png}
    \caption{So sánh prediction trên test case 2.}
\end{figure}

Case 3 có một quân trắng sát mép trên. YOLO và Faster R-CNN đều phát hiện đúng \texttt{white-bishop}, lần lượt với confidence khoảng 0.82 và 0.99. DETR không tạo prediction nào ở case này, cho thấy mô hình vẫn có thể bỏ sót object đơn lẻ sát biên ảnh.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/comparison_case_3.png}
    \caption{So sánh prediction trên test case 3.}
\end{figure}

Case 4 là một quân trắng đứng sát mép trên, ground truth tương ứng với \texttt{white-rook}. DETR phát hiện đúng \texttt{white-rook} với confidence 0.70, còn Faster R-CNN phát hiện rất chắc với confidence 1.00. YOLO không vẽ box trong case này. So với case 3, kết quả này cho thấy DETR không phải luôn thất bại với object sát biên, nhưng độ tự tin thấp hơn Faster R-CNN khá rõ.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/comparison_case_4.png}
    \caption{So sánh prediction trên test case 4.}
\end{figure}

Case 5 là ảnh khó nhất trong nhóm so sánh, có rất nhiều quân cờ, nhiều quân sát nhau và nhiều nhãn bị chồng lên nhau khi vẽ. Cả ba mô hình đều phát hiện được phần lớn object chính. Faster R-CNN cho confidence rất cao ở gần như toàn bộ box, thường đạt 1.00; DETR cũng giữ được nhiều box với confidence cao, đa số khoảng 0.89--1.00; YOLO phát hiện dày nhưng confidence phân tán hơn, có nhiều box quanh 0.86--0.95 và một vài dự đoán thấp hơn như \texttt{white-rook} khoảng 0.57 hoặc \texttt{black-rook} khoảng 0.70.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/comparison_case_5.png}
    \caption{So sánh prediction trên test case 5.}
\end{figure}

\subsubsection{Đường cong đánh giá}

Ba đường cong dưới đây được tính trên cùng tập test và cùng dải confidence threshold, nên có thể dùng để so sánh trực tiếp. YOLO giữ precision rất cao, khoảng 0.97--0.99 trên phần lớn dải ngưỡng; recall gần 0.99 đến khoảng threshold 0.7--0.8, sau đó giảm nhanh khi threshold vượt 0.85 và còn rất thấp ở ngưỡng cao nhất. DETR cân bằng hơn ở vùng threshold trung bình: precision gần 0.98--0.99, recall duy trì quanh 0.97--0.98 đến khoảng 0.65 rồi giảm dần, nhưng vẫn cao hơn YOLO ở đoạn cuối trước khi tụt mạnh tại ngưỡng rất cao. Faster R-CNN ổn định nhất, precision gần như cố định ở khoảng 0.987, recall bằng 1.000 và F1 khoảng 0.993 trên toàn dải threshold được vẽ.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/yolo_eval_curve.png}
        \small YOLO
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/detr_eval_curve.png}
        \small DETR
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/faster_rcnn_eval_curve.png}
        \small Faster R-CNN
    \end{minipage}
    \caption{So sánh đường cong đánh giá của ba mô hình trên cùng tập test.}
\end{figure}

\subsubsection{Failure case}

Hai panel lỗi giúp nhìn rõ hơn các tình huống gây nhầm hoặc bỏ sót. Ở failure case 1, ground truth là \texttt{white-rook}. Faster R-CNN dự đoán đúng với confidence 1.00, DETR cũng dự đoán đúng nhưng confidence thấp hơn, khoảng 0.70, còn YOLO bỏ sót hoàn toàn. Điều này cho thấy object sát mép ảnh vẫn là tình huống nhạy cảm: Faster R-CNN ổn định nhất, DETR có thể phát hiện nhưng kém chắc hơn, còn YOLO có thể bị mất object ở ngưỡng hiển thị hiện tại.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/failure_case_1.png}
    \caption{Failure case 1: ảnh một quân trắng sát mép trên.}
\end{figure}

Ở failure case 2, ảnh có nhiều quân cờ và các box nằm sát nhau. Ground truth dày đặc khiến chữ và box chồng lên nhau, nhưng vẫn thấy cả ba mô hình đều phát hiện được đa số quân. Khác biệt chính nằm ở độ tự tin và độ sạch của nhãn: Faster R-CNN gần như gán confidence 1.00 cho hầu hết quân, DETR cũng giữ nhiều box với confidence cao quanh 0.89--1.00, còn YOLO phủ được nhiều quân nhưng có một số dự đoán thấp hơn như \texttt{white-rook} 0.57 hoặc \texttt{black-rook} 0.70. Đây là tình huống thể hiện rõ ảnh hưởng của occlusion, mật độ quân cao và sự giống nhau về hình dạng giữa các lớp.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/failure_case_2.png}
    \caption{Failure case 2: ảnh nhiều quân cờ, nhiều box sát nhau.}
\end{figure}

\subsubsection{Tổng hợp nhận xét}

Từ toàn bộ các hình so sánh, có thể rút ra ba điểm chính:
\begin{itemize}[leftmargin=1.2cm]
    \item YOLO học nhanh và có metric huấn luyện tốt, nhưng trong ảnh test cụ thể vẫn có bỏ sót object sát biên như case 4/failure case 1 và một số confidence thấp ở ảnh nhiều quân.
    \item DETR phát hiện được nhiều object ở ảnh dày đặc và có đường cong đánh giá tốt, nhưng vẫn có case bỏ sót object đơn lẻ như case 2 và case 3; ở case 4 mô hình phát hiện đúng nhưng confidence chỉ khoảng 0.70.
    \item Faster R-CNN là mô hình ổn định nhất trong các hình hiện tại: confusion matrix sạch, đường cong đánh giá gần như không đổi theo threshold và các prediction mẫu thường có confidence 0.99--1.00.
\end{itemize}

Vì bài toán có phân bố lớp lệch, các lớp hiếm vẫn là điểm yếu chung của cả ba mô hình. Do đó, nhận xét chính của báo cáo không chỉ nằm ở số lượng box đúng, mà còn ở độ ổn định và khả năng giữ object trong các tình huống khó.

\subsection{Bảng tổng hợp nhận xét}

\begin{longtable}{p{0.18\textwidth}p{0.18\textwidth}p{0.54\textwidth}}
\toprule
\textbf{Mô hình} & \textbf{Đặc điểm nổi bật} & \textbf{Nhận xét} \\
\midrule
YOLO & One-stage, nhanh, trực quan & Hội tụ nhanh và có metric validation cao. Tuy nhiên ở các ảnh test cụ thể vẫn có trường hợp bỏ sót object sát biên và một số dự đoán confidence thấp trong ảnh đông quân. \\
DETR & Transformer, set prediction & Pipeline hiện đại, phát hiện được nhiều quân trong ảnh đông object và có eval curve tốt, nhưng vẫn nhạy với object đơn lẻ hoặc sát biên; có case bỏ sót hoàn toàn. \\
Faster R-CNN & Two-stage, ổn định & Ổn định nhất trong bộ hình hiện tại: confusion matrix sạch, confidence cao ở nhiều case và không tạo false positive trên ảnh gần trống. \\
\bottomrule
\end{longtable}

Nhìn chung, YOLO có lợi thế về tốc độ và quá trình train dễ theo dõi, Faster R-CNN cho chất lượng trực quan ổn định nhất trong các hình đã xuất, còn DETR thể hiện đúng tinh thần của một mô hình transformer hiện đại nhưng cần điều chỉnh thêm để giảm bỏ sót ở object đơn lẻ hoặc sát biên. Trên bộ dữ liệu có phân bố lớp lệch như bài toán cờ vua, các lớp hiếm vẫn là điểm khó chung của cả ba mô hình.

\section{Kết luận}

Bài thực hành 03 đã hoàn thành việc xây dựng pipeline phát hiện đối tượng trên bộ dữ liệu Chess Pieces với ba mô hình YOLO, DETR và Faster R-CNN. Nhóm đã chuẩn hóa dữ liệu sang các định dạng cần thiết, huấn luyện các mô hình, xuất hình prediction và tổng hợp kết quả để so sánh trực quan.

Qua thực nghiệm 40 epoch, nhóm quan sát được ba điểm chính. Thứ nhất, bộ dữ liệu có tính mất cân bằng rõ rệt nên các lớp hiếm khó được mô hình học tốt như các lớp xuất hiện nhiều. Thứ hai, YOLO học nhanh và đạt metric validation tốt, nhưng vẫn có thể bỏ sót object sát biên hoặc tạo prediction confidence thấp trong ảnh nhiều quân. Thứ ba, DETR và Faster R-CNN đều mang lại góc nhìn thú vị về detection hiện đại, trong đó DETR cho thấy đặc trưng end-to-end nhưng vẫn nhạy với object đơn lẻ, còn Faster R-CNN thể hiện sự ổn định của detector hai giai đoạn qua confusion matrix, eval curve và prediction mẫu.

Về mặt học thuật, bài thực hành giúp nhóm hiểu rõ hơn sự khác nhau giữa các họ mô hình phát hiện đối tượng, vai trò của định dạng annotation và ảnh hưởng của ngưỡng confidence khi trực quan hóa kết quả. Trong ứng dụng thực tế, việc chọn mô hình sẽ phụ thuộc vào yêu cầu về tốc độ, độ ổn định và mức độ phù hợp với dữ liệu đầu vào.

\end{document}
